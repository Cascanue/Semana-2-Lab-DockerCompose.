version: '0'

services:
 app1:
  build: .
  container_name: app1
  ports:
   - "3000:3000"
  environment:
   - MESSAGE=${MESSAGE}
app2:
   container_name: app2
   build: .
   ports:
    - "3001:3000"
   environment: 
    - MESSAGE=${MESSAGE}
app3:
   container_name: app3
   build: .
   ports:
    - "3002:3000"
   environment: 
    - MESSAGE=${MESSAGE}
